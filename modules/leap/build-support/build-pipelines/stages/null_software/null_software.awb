%name Null software build tool
%desc Build for software side of hybrid system

%attributes tool

%provides software_tool

%notes README

%scons %library software_null.py

%param --global BUILD_FIRST_PASS_SOFTWARE 0 "Should we build software during first pass build? (For legacy compilation only)."

